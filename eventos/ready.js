const Discord = require("discord.js");

// Emitido quando o bot fica pronto
module.exports = async (client) => {
    client.log("bot", `${client.user.tag} pronto!`);

    // TODO O coisa do cargos la
}